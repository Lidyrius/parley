Pre-rendered Jarvis acknowledgement PCM clips land here, one folder per intent
(generate-line-clips.sh). At least this placeholder keeps the SPM resource dir
non-empty so `.copy("Resources/lines")` builds on a fresh checkout (the *.pcm are
gitignored and generated at install).
